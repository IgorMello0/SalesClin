import { prisma } from './server/prisma'; async function run() { const user = { id: 5, type: 'profissional' }; let companyId; const prof = await prisma.professional.findUnique({ where: { id: user.id }, select: { companyId: true } }); companyId = prof?.companyId || undefined; const where = {}; if (companyId) { const companyProfessionals = await prisma.professional.findMany({ where: { companyId }, select: { id: true } }); where.professionalId = { in: companyProfessionals.map(p => p.id) }; } console.log('where:', JSON.stringify(where)); const clients = await prisma.client.findMany({ where }); console.log('clients:', clients); } run();
