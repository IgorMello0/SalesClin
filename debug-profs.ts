import { prisma } from './server/prisma'; async function main() { const profs = await prisma.professional.findMany({ select: { id: true, name: true, companyId: true, companyName: true } }); console.table(profs); const clients = await prisma.client.findMany({ select: { id: true, name: true, professionalId: true }}); console.table(clients); } main().finally(() => prisma.$disconnect());
